% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/miRTS_score.R
\name{compute_mcp_mirna_scores}
\alias{compute_mcp_mirna_scores}
\title{Compute MCP-style tissue scores from miRNA counts}
\usage{
compute_mcp_mirna_scores(
  expr_tpm,
  ts_df,
  log2_transform = TRUE,
  pseudo_count = 1,
  center_genes = TRUE,
  min_markers = 2,
  summary_fun = c("median", "mean")
)
}
\arguments{
\item{expr_tpm}{Matrix or data.frame of miRNA expression (e.g., TPM),
rows = miRNAs, cols = samples.}

\item{ts_df}{Data frame with columns:
- miRNA: miRNA ID matching rownames(expr_tpm)
- enriched_organ: tissue/organ name
Default to ts_miRNA, which records the enriched tissues for each miRNAs in the \code{miRTS_signature_v1}}

\item{log2_transform}{Logical; log2(TPM + pseudo_count) if TRUE.}

\item{pseudo_count}{Pseudo-count added before log2 transform.}

\item{center_genes}{Logical; median-center each miRNA across samples.}

\item{min_markers}{Minimum number of markers required for a tissue.
If fewer, scores are set to NA for that tissue.}

\item{summary_fun}{"median" (default) or "mean" for marker aggregation.}
}
\value{
Matrix: tissues (rows) × samples (columns) of MCP-style scores.
}
\description{
Compute MCP-style tissue scores from miRNA counts
}
